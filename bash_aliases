# Stackato client aliases
alias st="stackato"

# vagrant aliases
alias vu="vagrant up"
alias vs="vagrant ssh"
alias vd="vagrant destroy"

# ls aliases
alias ls='ls --color=auto'
alias l='ls'
alias ll='ls -al'
alias la='ls -A'
